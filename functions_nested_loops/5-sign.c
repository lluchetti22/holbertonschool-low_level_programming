#include <stdio.h>
#include "main.h"

/**
* _isalpha - print numbers
* @n: a int
* Return: 0
*/

int print_sign(int n)
{

if (n > 0)
{
return (1);
}
else if (n < 0)
{
return (-1);
}
else
{
return (0);
}

}